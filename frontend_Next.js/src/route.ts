export const adminRoutes = ["/dashboard"];

export const publicRoutes = ["/", "/about"]; // add more if needed

export const authRoutes = ["/auth/signin", "/auth/signup", "/auth/verify-2fa"];

export const DEFAULT_SIGNIN_REDIRECT = "/";
